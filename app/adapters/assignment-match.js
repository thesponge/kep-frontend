import ApplicationAdapter from './application';

export default ApplicationAdapter.extend({
  //url: "/v1/score_account_assignments/assignment_matches",
  //namespace: ENV.APP.apiNamespace + "/score_account_assignments",
  namespace: "/v1/score_account_assignments",
});
