import { moduleFor, test } from 'ember-qunit';

moduleFor('view:assignments/show', 'Unit | View | assignments/show');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
