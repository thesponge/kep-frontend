import { cutString } from '../../../helpers/cut-string';
import { module, test } from 'qunit';

module('Unit | Helper | cut string');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = cutString(42);
  assert.ok(result);
});
